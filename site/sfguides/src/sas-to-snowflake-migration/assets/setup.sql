-- Run this worksheet once on a fresh Snowflake trial account before starting the lab.
-- All converted notebooks read from and write to LDEMO.PUBLIC.

CREATE DATABASE IF NOT EXISTS LDEMO;
USE DATABASE LDEMO;
USE SCHEMA PUBLIC;
