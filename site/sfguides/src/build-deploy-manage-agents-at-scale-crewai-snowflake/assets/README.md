This folder contains image assets for the guide `build-deploy-agents-crewai-snowflake.md`.

## Current status

The repository did not include the original screenshot files referenced by the guide.

## Current assets used by the guide

- `intro.jpg`: downloaded public thumbnail for the referenced video
- `crewai-amp.jpg`: extracted frame showing the CrewAI visual editor (flow view)
- `crew-studio.jpg`: extracted frame showing the CrewAI execution/events view
- `deep-research.jpg`: extracted frame showing a “deep research” style prompt in the UI

The `.svg` files in this folder are placeholders from earlier iteration and are no longer referenced by the guide.

## Replacing placeholders with real screengrabs

If you have the actual screengrabs, replace these files (keep the same filenames and paths):

- `intro.svg`
- `crewai-amp.svg`
- `crew-studio.svg`
- `deep-research.svg`

If you prefer JPG/PNG screengrabs, you can:

1. Add `intro.jpg` / `intro.png` (etc.) into this folder
2. Update the image references in the guide to match the new extensions

