We write feedback and proofreads here, which becomes prompt input for AI to edit files consistently:
- narration sets the story (inspired by the blog-post.md), how-to.md says how to do using UI AND sql code, the notebook implements it with mixed sql, python, and markdown cells. The quickstart- implements it for official snowflake guides.

So, all files are related and telling the same "narration" and must match.

The [X] indicates the feedback is already applied before.

- [X] Always use official names. At least at the first apprearance (or second) must be offical like Snowflake Cortex Agent or Google Cloud Lakehouse Runtime Catalog. Later can be called Cortex Agent and Runtime Catalog as it is clear.


- [X] Use current namings like Gemini Enteprise, Agent Builder Platfrom (instead of vertex ai), ... whenver needed.

("" means inserted. ~~ means retracted.)

Feedback from Zoe: 
- [X] I already applied feedback from Zoe into the narration.md. Please review and follow.


- [X] Feedback for how-to codes from snowflake guides code review.

