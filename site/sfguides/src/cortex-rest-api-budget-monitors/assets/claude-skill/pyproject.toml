[project]
name = "cortex-usage-report"
version = "0.1.0"
description = "Generate Cortex REST API usage PDF reports"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8.0",
    "reportlab>=4.0.0",
    "pandas>=2.0.0",
]
